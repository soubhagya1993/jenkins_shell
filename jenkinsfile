pipeline {
    agent any

//    parameters {
//        choice(name: 'branch', choices: ['main','dev'])
//    }

    stages {
        stage('execute shell') { 
            steps {
                sh 'sum=`expr $a + $b`'
                sh 'echo $sum'
            }
        }
    }
}
